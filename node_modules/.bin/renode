#!/bin/sh
if [ -x "`dirname "$0"`//usr/local/bin/node" ]; then
  "`dirname "$0"`//usr/local/bin/node"  "`dirname "$0"`/../renode/renode" "$@"
  ret=$?
else 
  /usr/local/bin/node  "`dirname "$0"`/../renode/renode" "$@"
  ret=$?
fi
exit $ret
